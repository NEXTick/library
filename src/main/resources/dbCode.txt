create table Person
(
    id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name varchar(50) NOT NULL,
    yearOfBirthday  int CHECK (yearOfBirthday > 1850)
);

create table Book
(
    id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title varchar(100) NOT NULL,
    author varchar(100) NOT NULL,
    year int
);

CREATE TABLE Person_Book(
    person_id int REFERENCES Person(id) ON DELETE SET NULL,
    book_id int REFERENCES Book(id),
    PRIMARY KEY (person_id, book_id)
);

SELECT Book.title, Book.author, Book.year FROM Book
    JOIN Person_Book on Book.id = Person_Book.book_id
    JOIN Person on Person_Book.person_id = Person.id where person_id = ?;



INSERT INTO Person(name, yearOfBirthday) VALUES ('Rukovitsyn', 1997);
INSERT INTO Person(name, yearOfBirthday) VALUES ('Fadeev', 1993);
INSERT INTO Person(name, yearOfBirthday) VALUES ('Erofeev', 1997);
INSERT INTO Person(name, yearOfBirthday) VALUES ('Hazov', 1953);

INSERT INTO Book(title, author, year) VALUES ('Мастер и Маргарита', 'Булгаков М.А.', 1966);
INSERT INTO Book(title, author, year) VALUES ('Война и мир', 'Толстой Л.Н.', 1865);
INSERT INTO Book(title, author, year) VALUES ('Преступление и наказание', 'Достоевский Ф.М.', 1866);
INSERT INTO Book(title, author, year) VALUES ('Собачье сердце', 'Булгаков М.А.', 1925);


INSERT INTO Person_Book VALUES (1,1);
INSERT INTO Person_Book VALUES (1,2);
INSERT INTO Person_Book VALUES (2,3);
INSERT INTO Person_Book VALUES (3,4);